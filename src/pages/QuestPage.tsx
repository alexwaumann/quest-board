import {
  Grid,
  Typography,
} from '@mui/material';

const QuestPage = () => {

  return (
    <Grid container spacing={4}>
      <Grid item xs={12}>
        <Typography variant="h6">Quests</Typography>
      </Grid>
    </Grid>
  );
};

export default QuestPage;
